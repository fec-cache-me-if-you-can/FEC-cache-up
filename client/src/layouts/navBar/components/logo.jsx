import React from 'react';
import PropTypes from 'prop-types';

const Logo = ({ color = '#222222', height = 100 }) => {
  const width = String(height * 2.85463918);
  height = String(height);
  return (
    
  );
};
Logo.propTypes = {
  color: PropTypes.string,
};
export default Logo;
